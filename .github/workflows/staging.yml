name: Staging Check

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Confirm FORGE files pushed
        run: echo "FORGE workflow files pushed successfully"
